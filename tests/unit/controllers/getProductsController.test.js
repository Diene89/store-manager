// const getProductsController = require('../../../services/getProductsController');
// const getProductsService = require('../../../models/getProductsService');
// const chaiAsPromised = require('chai-as-promised');
// const { ValidationError } = require('joi');
// const sinon = require('sinon');
// const { expect, use } = require('chai');
// const { listProducts, product } = require('../mocks/productsMock');

// use(chaiAsPromised);

// describe('', () => {
//   beforeEach(() => sinon.restore());
//   describe('', () => {
//     it('caso o service devolva um array, a res.status deve ser 200 e res.json com a array', () => {
//       sinon.stub(getProductsService, 'getProducts').resolves(listProducts);
//       const req = {};
//       const res = {};
//       res.status = sinon.stub().returns(res);
//       res.json = sinon.stub();

//       getProductsController.getProducts(req, res);
//     })
//   })
// })
